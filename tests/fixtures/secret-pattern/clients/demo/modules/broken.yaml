# Invalid fixture: a private-key header is committed in a description. The secret
# scanner must reject this. A bare header (no key body) is used so the fixture
# itself is not a real leaked credential.
schema_version: "0.1"
kind: ontology_module
id: demo.brand
title: "Demo brand"
client_id: demo
status: active
workstreams: [brand]
evidence_sources:
  - id: demo-src
    type: local_project_doc
    path: "docs/demo.md"
    description: "-----BEGIN PRIVATE KEY----- (fixture: must be rejected)"
entities:
  - id: demo.brand.voice
    label: "Voice"
    entity_type: brand_object
    description: "Brand voice."
    source_confidence: draft
relationships: []
rules: []
